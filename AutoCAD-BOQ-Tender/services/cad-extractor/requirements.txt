ezdxf==1.3.5
# Pillow is required by ezdxf.addons.drawing (the SVG render backend imports it);
# it's only a *transitive* dep of ezdxf so it must be pinned explicitly, or a
# fresh install lacks it and SVG rendering fails with "No module named 'PIL'".
Pillow>=10,<13
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
# PyMuPDF: PDF text extraction with coordinates. AGPL-licensed; fine for
# internal/server-side use. If you need to distribute the platform as a closed
# product, swap to pdfplumber + pypdf (slower, but MIT).
# 1.27.x has prebuilt wheels for Python 3.13; 1.24.10 did not and tried to
# compile MuPDF from source (requires Visual Studio on Windows).
PyMuPDF>=1.27.0,<1.28
